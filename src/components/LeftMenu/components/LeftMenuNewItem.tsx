function LeftMenuNewItem() {
  // TODO: Implement the functionality for creating a new item in the left menu.
  // 1. Input field for the new item name. When the use miss focus or presses Enter, the new item should be created.
  // If value is empty, the item should not be created.
  // 2. Add validation to ensure the new item name is not empty, and less than 50 characters.
  // 3. Switch to the new folder after it is created.



  return ()
}

export default LeftMenuNewItem;